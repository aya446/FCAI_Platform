import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF000E30);
  static const Color secondColor = Color(0xFFD0DFF1);
  static const Color grey = Color(0xFF6C6969);
  static Color whiteWithOpacity = Colors.white.withValues(alpha: 0.7);
}
